# BabyHippo - Core Dependencies
# =============================
# 최소 필수 의존성만 포함
# 전체 의존성은 pyproject.toml 참조

numpy>=1.20.0

# Optional Dependencies (선택적)
# --------------------------------
# LLM 통합을 원하면:
# openai>=1.0.0
# anthropic>=0.18.0

# 블록체인 통합을 원하면:
# web3>=6.0.0
# python-dotenv>=1.0.0

# 전체 기능을 원하면:
# pip install -e ".[all]"

